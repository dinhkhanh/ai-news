{
  "name": "@ai-news/media-lambda",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/handler.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/handler.mjs --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run",
    "deploy": "bash deploy.sh",
    "build:server": "esbuild src/server.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/server.mjs --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "build:nas": "bash build-nas.sh"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1131.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^22",
    "esbuild": "^0.25.0",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^5"
  }
}
