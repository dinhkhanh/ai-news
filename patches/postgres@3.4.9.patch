diff --git a/cf/src/connection.js b/cf/src/connection.js
index 8e79170aeb13efe8c3c77ccede0cd3a115b5e1b5..470ecced7bdf5613aa11699f8c2cc8c9e8909a03 100644
--- a/cf/src/connection.js
+++ b/cf/src/connection.js
@@ -172,11 +172,13 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
         : (query = q, query.active = true)
 
       build(q)
-      return write(toBuffer(q))
+      // patched: always run onexecute (it reserves the connection for sql.begin), so max_pipeline: 0 works
+      const more = write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
+      q.options.onexecute && q.options.onexecute(connection)
+      return more && !q.options.onexecute
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
diff --git a/cjs/src/connection.js b/cjs/src/connection.js
index 07f6716702ac888215c86ad6e0071aaf55ae519c..09332951d07415feaff217fb67d9b1cacec59797 100644
--- a/cjs/src/connection.js
+++ b/cjs/src/connection.js
@@ -170,11 +170,13 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
         : (query = q, query.active = true)
 
       build(q)
-      return write(toBuffer(q))
+      // patched: always run onexecute (it reserves the connection for sql.begin), so max_pipeline: 0 works
+      const more = write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
+      q.options.onexecute && q.options.onexecute(connection)
+      return more && !q.options.onexecute
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
diff --git a/src/connection.js b/src/connection.js
index 1b1cccde43b4570d5d071d6ffaab2669b3c2065a..082a6bf198a48b52f9d59012d85c8bfeb2a0974b 100644
--- a/src/connection.js
+++ b/src/connection.js
@@ -170,11 +170,13 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
         : (query = q, query.active = true)
 
       build(q)
-      return write(toBuffer(q))
+      // patched: always run onexecute (it reserves the connection for sql.begin), so max_pipeline: 0 works
+      const more = write(toBuffer(q))
         && !q.describeFirst
         && !q.cursorFn
         && sent.length < max_pipeline
-        && (!q.options.onexecute || q.options.onexecute(connection))
+      q.options.onexecute && q.options.onexecute(connection)
+      return more && !q.options.onexecute
     } catch (error) {
       sent.length === 0 && write(Sync)
       errored(error)
